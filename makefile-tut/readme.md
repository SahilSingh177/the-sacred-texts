`make` -> Automatically detects the `Makefile` and runs it
`make -f Makefile-1` -> Runs the makefile `Makefile-1`.
`make -f Makefile-1 CC=gcc` -> Runs the makefile with variable value as mentioned.
`make -f Makefile clean` -> Removes all object and executables
``